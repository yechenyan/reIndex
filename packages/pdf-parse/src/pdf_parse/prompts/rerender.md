# Updated whole-table screenshot

Runtime has applied your screenshot clarity request and attached the replacement
overview and whole-table image. The native geometry revision is unchanged and is
not repeated. Restart the strict one-response order: finish and freeze the visual
sample from this new screenshot, then generate the parser from the native geometry
already in this session. Return complete scripts and the same
`geometryRevisionUsed`. If still unreadable, one further different DPI request is
allowed inside `dpiBounds`; never use OCR or per-row crops.
