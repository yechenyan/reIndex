# reindex-server

Backend HTTP service for ReIndex storage and query APIs.

```bash
uv run reindex-server run
```

The initial service exposes `GET /health`. Storage, indexing, and query endpoints
will be added after their protocol tasks are reviewed.

