# reindex-cli

Python client and command-line entrypoint for ReIndex workflows.

```bash
uv run reindex doctor
```

The initial package exposes installation diagnostics. Build, upload, search,
browse, get, and query commands will be added as their contracts are implemented.

