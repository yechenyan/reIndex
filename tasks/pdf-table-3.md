# PDF Table 3

状态：方案设计，等待人工审核

## 用户请求

> 在 `packages/pdf-table-3/plan.md` 输出最终方案，包括流程图、角色、协议和细节；随后明确不受 200 行偏好限制，要求不要为了行数压缩设计细节。

## 当前范围

- 仅编写新工作流的设计方案，不开发实现。
- 新包从零设计，不依赖既有 PDF 表格提取包；旧包只作基线。
- 文档需要覆盖 main、调度程序、固定代码、持续 Finder/Parser/QA、Parser 内部滚动合并、ready 文件并行、直接产物、diff、恢复与发布。

## 文件长度例外说明

`packages/pdf-table-3/plan.md` 是本工作流唯一的总体设计规格，需要把流程、角色边界、文件合同、状态恢复、错误路由和验收条件放在同一可审核上下文中。拆分会削弱协议与流程的一致性检查，因此允许超过 200 行；仍遵守仓库绝对上限，不超过 300 行。

## 验收

- 用户审核 `plan.md` 后再决定是否开工。
- 未经用户明确批准，不归档、不标记完成。
