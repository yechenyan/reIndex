# CLI 契约优先与统一文档

## 状态

等待人工审核

## 用户原始请求

> 按这个方案做吧，做完户跑完全部的单元测试，并以 reIndex/testbase/test2-generage 这个为例，怕一遍流程。

## 追加请求

> review 下改动

> 修复下

本轮修复审查发现的可执行约束、返回 Schema、兼容测试和行数上限问题。

## 范围

- 建立单一、机器可读的 ReIndex CLI v1 权威契约。
- 让命令树、参数、默认值、约束和成功输出校验由契约驱动。
- 将 CLI 执行入口改为契约构造的 Click 命令树，业务 handler 不直接注册接口。
- 拆分现有超长 `cli.py`，保留当前命令路径、参数和业务逻辑。
- 在 Web App 增加 Doc 父入口、CLI Reference 和 HTTP API Reference。
- 增加契约一致性、生成产物和兼容回归测试。
- 跑全量单元测试，并以 `testbase/test2-generage` 执行真实 CLI 流程。

## 边界与兼容要求

- 不修改现有命令名称、参数名称、默认值、choices 或业务返回对象。
- 当前成功输出仍为 stdout JSON，业务错误仍为 stderr JSON；参数错误保持退出码 2。
- Click 只是契约执行器，手写 handler 不得声明命令、参数或选项。
- Web 文档只读取权威契约的编译产物，不成为第二份事实来源。
- 当前工作树包含大量用户既有修改；本任务只增量编辑 CLI 契约、执行入口、Web 文档及相关测试和文档。

## 验收检查

- 修改 CLI 接口必须先修改权威契约，否则一致性检查失败。
- 契约命令和 handler 一一对应，成功输出经过 JSON Schema 校验。
- 新建或实质修改的手写文件不超过 200 行；特殊情况不超过 300 行并记录理由。
- CLI/API 文档可从 Web App 的 Doc 父入口访问。
- 现有 CLI 测试、全量单元测试和 Web 构建执行并记录结果。
- `testbase/test2-generage` 的 inspect、scan、check、push、search/get 或适用的完整流程执行并记录结果。

## 实现结果

- 权威契约：`packages/cli/src/reindex_cli/contract/reindex-cli-v1.yaml`。
- `cli_builder.py` 只从契约构造 Click 命令树；拆分后的 handler 只实现业务。
- `scripts/compile_cli_contract.py` 校验 handler 一一覆盖，并生成
  `packages/web-app/public/doc/cli-v1.json`。
- Web App 已增加 `#/doc`、`#/doc/cli`、`#/doc/api`；CLI 页面读取生成
  artifact，API 页面读取服务端 `/openapi.json` 并用 Scalar React 渲染。
- 浏览器检查三个页面均能渲染；显式补入 Scalar CSS 后无控制台错误。
- `AGENTS.md` 已加入 CLI 契约优先顺序、兼容规则和强制检查命令。
- CLI 约束现为契约中的结构化规则，由统一执行器处理；usage 错误保持退出码 2，
  business 错误保持退出码 1。
- 17 个命令都引用命令级成功返回 Schema，dispatch 在输出前按对应 Schema 校验。
- CLI v1 命令路径、参数、默认值、choices、约束和输出 Schema 引用已冻结为兼容指纹，
  防止后续实现无意改坏现有接口。
- 审查发现的超长源码和测试已按职责拆分，没有删除断言或业务逻辑；Raw authoring
  细节已从 package 主协议迁回独立输入协议。

## 验证记录

- `uv run python scripts/compile_cli_contract.py`：生成成功；随后 `--check` 通过。
- `uv run python scripts/check_http_contract.py`：通过，13 paths。
- `pnpm build`：通过，5708 modules transformed。
- 全量 `uv run pytest -q`：74 passed、2 skipped，0 failed。
- 修复后的 CLI/服务/PDF 定向回归：24 passed；版本化 HTTP/CLI 流程：4 passed。
- Python `compileall` 与本任务范围 `git diff --check`：通过。Web 包没有单独的
  `lint` script，TypeScript 校验已由 `pnpm build` 中的 `tsc -b` 完成。
- `testbase/test2-generage` 的临时副本重新执行 `inspect -> scan -> check`：
  3 CSV、1 PDF、2 relations、12 Nodes、0 warnings，全部 valid。
- API 与 Web 已重启并完成健康检查；OpenAPI 和生成的 CLI 文档产物均可读取。

## test2-generage 完整流程

- 先在 `/tmp` 副本执行 `inspect -> scan -> check`，确认 3 CSV、1 PDF、
  2 relations、12 Nodes、0 warnings；随后在原 fixture 重新生成并检查通过。
- 对 `http://127.0.0.1:8000` 执行 push dry-run 和实际 push；实际发布
  12 Nodes、26 resources、1186 search units，生成版本
  `46286c5c-d89e-47d7-8c47-f87483a53e9a`。
- `fetch`、`history`、远端 lexical `search`、远端 `get`、Node-only `pull`
  全部通过；pull 结果为 12 Nodes。

## 行数例外

- `wiki/reference/reindex-v1.0-standard.md`（296 行）是完整 package 协议，继续作为
  特殊文档保留在 300 行以内；Raw authoring 细节已拆到独立输入协议，避免超过上限。
- `wiki/reference/reindex-input-v1.0.md`（234 行）是完整输入协议，保留连续章节和既有
  锚点作为特殊文档，且低于 300 行。其余本任务新增或实质修改的手写文件均不超过
  200 行；生成契约 artifact 与 lockfile 按规则豁免。
