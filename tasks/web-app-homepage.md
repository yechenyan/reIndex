# Web app 首页

状态：等待人工审核

## 用户请求

> 了解下 reIndex 项目，给 reIndex/packages/web-app 做个首页，我希望首页做成这些看版，描述它的主要功能，支持多语言。

> 首页再多点内容

## 范围与假设

- 在现有 React 单页应用中新增默认首页，不调整后端或 API 契约。
- 首页以看板形式概述 ReIndex 的导入、索引、检索、浏览、表格查询、CLI/API 与 Agent 工作流。
- 复用现有德语、英语、中文语言切换与视觉风格。

## 验收检查

- 打开根地址时显示首页，并可进入现有 Explore、Search 与文档页面。
- 首页文本随语言选择器切换。
- 桌面及窄屏布局可用，前端 TypeScript 与生产构建通过。

## 实施与验证

- 新增 `#/` 默认路由和产品首页；品牌标识回到首页，导航中加入首页入口。
- 首页以三个看板说明 ReIndex 的资料准备、可追溯检索，以及面向人和 Agent 的使用方式，并提供到现有功能页的入口。
- 根据后续请求扩展首页，补充四步工作流、可追溯性说明与 CLI/API 行动入口。
- 为首页全部文案补齐德语、英语和中文翻译，复用原有语言选择与本地保存机制。
- 已运行 `pnpm build`（在 `packages/web-app`）：TypeScript 检查和 Vite 生产构建通过；扩展内容后再次运行并通过。
- 已在本地预览验证英文及中文首页；在 390px 宽度下，看板由三列切换为单列。

## 文档检查

- 已更新 `packages/web-app/README.md` 的页面列表，加入首页入口。
- 根 README 和 wiki 对产品能力的描述仍与本次前端入口一致，无需修改。
