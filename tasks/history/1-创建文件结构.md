我要做一个项目，功能见： reIndex/wiki

代码实现：
1. 用python
2. 依赖管理用 uv
3. cli 和发布部署参考项目 loom

文件结构：
- packages 里面的独立包
  - cli reIndex CLI 工具，负责触发上传数据库，查找数据等工具
  - server 后端服务
- testbase 测试的地方， 包括测试的数据，测试生成的 reIndex， 里面由可以拆分成多个子文件
- wiki 文档
- task 认为， 完成的任务请放到 history 里， 你写个项目级的 skill 经过人审核后文档这么做

你的任务：
1. 创建文档结构
2. 初始化git, git https://github.com/yechenyan/reIndex.git
3. 先实现第一步在 testbase/test1 里放入原始文件，和你新生成 reIndex
   原始文件用 nap_gridextractor/data/task_outputs/vnb/pfalzwerke/task_download_nap/nap_2024_angepasste_fassung_vom_07_08_2024.pdf。 生成的 reIndex 你直接自己想办法生成
4. 写关于 task 的skill， 

你有什么问题么？

---

## 状态

已完成

## 返工记录

### 人工反馈（2026-07-26）

> reIndex/testbase/test1/reIndex 这个生成的不好，表格丢失了， 文档格式也不好，重新做吧啊。不要用这个做例子，用 /Users/maxiao/Documents/code2/nap_gridextractor/data/NAP/2022_07_28_netzausbauplan_bielefelder_netz_gmbh_2022_inkl_anhang_pdf.pdf 做例子

此前 Pfalzwerke 68 页样例未通过人工审核，已完全替换，不再作为 `test1`
内容或后续实现基线。

### 人工反馈（2026-07-26，第二轮）

> 06 和 08 是表格，不是图片

已修正数据建模：两个表格的 CSV 和高分辨率 PNG 现在归属于同一个
`table` Node；PNG 只是同一表格的视觉参考，不再建立独立 `image` Node。
子 Node 重新连续编号为 `0001` 至 `0006`。

## 实现结果

- 创建 Python 3.12 + uv workspace。
- 创建独立的 `packages/cli` 与 `packages/server` 包。
- CLI 提供 `reindex doctor`；后端提供 `reindex-server run` 和 `/health`。
- 创建 README、wiki reference/user/dev 文档结构以及 Render Blueprint。
- 初始化本地 Git，默认分支为 `main`，远程 `origin` 指向
  `https://github.com/yechenyan/reIndex.git`。
- 将指定的 Bielefelder Netz 五页 PDF 原字节复制到 `testbase/test1/raw/`，
  SHA-256 为 `3dc36a9917ac29b387c8fcc6e1a856f26e4fc0660d4e847a5070ee7dca0af497`。
- 创建可复现转换器，生成 8 个 `reindex/node@0.1` Node、2 个完整 CSV
  表格和 3 个视觉资源。
- 正文按语义章节生成 Markdown，并清理 PDF 换行断词。
- 聚合投资表规范化为 24 行长表；措施表按 PDF 的真实 20 列向量网格逐
  单元格提取，共保留 52 行，避免多行或多列粘连。
- 保留原始嵌入式网络图；它是唯一的 `image` Node。两个表格的高分辨率
  裁剪图作为各自 `table` Node 的视觉参考，用于核验但不重复建模。
- 已移除 `test1` 内旧 Pfalzwerke PDF 和旧生成结果；原文件仍保留在
  `nap_gridextractor` 的原始路径，可恢复。
- 创建并校验项目级 `$reindex-task-workflow` skill；任务只有在人工明确
  通过或确认完成后才移动到 `tasks/history/`。

## 验证

- `uv sync`
- `uv run --package reindex-cli python testbase/test1/build_reindex.py`
- `uv run pytest -q`：8 passed
- 连续重建后 13 个生成文件的 SHA-256 清单完全一致。
- `uv build --package reindex-cli`
- `uv build --package reindex-server`
- `uv run --with pyyaml python .../quick_validate.py
  .agents/skills/reindex-task-workflow`：Skill is valid
- `render.yaml` 通过 Render 官方 JSON Schema 校验。
- `render blueprints validate` 因本机 Render 会话未授权返回 401，未执行真实
  Blueprint API 校验，也未触发部署。

## 人工批准

- 2026-07-26，用户明确回复“任务1 完成”，批准当前结果归档。
- 新的 Bielefelder Netz 文本、表格 CSV 和视觉资源作为后续实现基线。

## 完成前文档审计

- 检查了根 README、wiki、package README、fixture README、生成示例、
  `AGENTS.md`、项目 skill 和本任务记录。
- 当前文档中没有遗留 `0007`、`0008`、10 Nodes、15 个生成文件或
  7 passed 等旧实现描述。
- 任务原始请求中的 Pfalzwerke 路径和返工记录作为历史上下文保留；
  测试中的旧路径和 `0008` 是明确的负向回归检查。
- 在协议文档中补充“表格裁剪图属于 `table` Node，不是独立
  `image` Node”的规则。
- 在任务开发指南、`AGENTS.md` 和 `$reindex-task-workflow` 中增加归档前
  文档一致性检查，并同步 Skill 界面描述。
- 再次执行 `uv run pytest -q`：8 passed。
- 再次执行 Skill `quick_validate.py`：Skill is valid。

## 完成结果

任务经人工批准、文档审计和验证后，已移入 `tasks/history/`。
